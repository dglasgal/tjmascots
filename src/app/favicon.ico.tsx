// NOTE: this placeholder file is not used by Next.js — the .ico.tsx extension
// isn't a Next convention. It exists only because we couldn't delete it from the
// sandbox. Safe to delete locally. Next will ignore it at build time.
export {};
